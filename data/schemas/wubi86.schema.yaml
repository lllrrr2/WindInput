schema:
  id: wubi86
  name: "五笔"
  icon_label: "五"
  version: "1.0"
  author: "内置"
  description: "五笔86版输入方案"

engine:
  type: codetable
  codetable:
    max_code_length: 4
    auto_commit_unique: false
    clear_on_empty_max: false
    top_code_commit: true
    punct_commit: true
    show_code_hint: true
    single_code_input: false
    candidate_sort_mode: frequency
    temp_pinyin:
      enabled: true
      schema: pinyin
  filter_mode: smart

dictionaries:
  - id: wubi86_main
    path: "dict/wubi86/wubi86_jidian.dict.yaml"
    type: rime_codetable
    default: true
    weight_spec:
      median: 10
      max: 890
      min: 5
      mode: linear

user_data:
  shadow_file: "wubi86.shadow.yaml"
  user_dict_file: "wubi86.userwords.txt"

learning:
  mode: manual
  protect_top_n: 3

encoder:
  max_word_length: 10
  rules:
    - length_equal: 2
      formula: "AaAbBaBb"
    - length_equal: 3
      formula: "AaBaCaCb"
    - length_in_range: [4, 10]
      formula: "AaBaCaZa"
