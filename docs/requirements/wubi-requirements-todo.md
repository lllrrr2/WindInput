# 五笔输入法需求匹配 TODO

> 基于 `docs/requirements/wubi-requirements.md` 与当前项目实现的对比分析，整理时间：2026-03-15

## 整体完成度：约 55-60%

---

## P0 必须支持（15项）

### 已实现 ✅

- [x] **R05** 四码唯一自动上屏可配置 — `WubiConfig.AutoCommitAt4`，默认关闭
- [x] **R06** 第五码顶字上屏可配置 — `WubiConfig.TopCodeCommit`，默认开启
- [x] **R07** 纯五笔模式 — 引擎类型 `wubi`，支持纯五笔
- [x] **R08** 候选数量可配置 — `UIConfig.CandidatesPerPage`，默认7，支持数字0选第10项
- [x] **R09** 分号/引号选重 — `SelectKeyGroups` 默认 `semicolon_quote`
- [x] **R10** 中英文切换键 — 默认左右Shift，支持Ctrl、CapsLock
- [x] **R11** 切换英文时编码处理 — 切换时提交当前编码

### 部分实现 🔶

- [ ] **R02** 自定义标点 — 标点映射硬编码在 `transform/punctuation.go`，需改为用户可配置
- [ ] **R03** Shift+字母行为可配置 — `ShiftTempEnglishConfig` 已有开关，但需确认是否支持"大写字母直接上屏"和"转入临时英文"两种模式切换
- [ ] **R12** 临时拼音/生僻字引导 — 临时拼音已实现（反引号触发），生僻字引导未实现
- [ ] **R13** 回车键行为 — 临时拼音模式下回车上屏原始字母；需确认正常模式是否可配置为"编码上屏"或"清空编码"

### 未实现 ❌

- [ ] **R01** 词库导入/导出 — 当前有词库分层管理但无导入导出功能，核心数据可迁移性缺失
- [ ] **R04** 数字后句号行为可配置 — 未发现"直接输出句号"和"自动转为小数点"的切换配置
- [ ] **R14** z键重复录入 — 未发现相关功能实现
- [ ] **R15** 单候选时选重键行为 — 未发现针对单候选场景的特殊处理（分号/引号不应起作用）

---

## P1 建议支持（9项）

### 已实现 ✅

- [x] **R17** 五笔拼音混输 — 有独立拼音引擎，可通过 Ctrl+` 切换
- [x] **R21** 生僻字词开关 — `filter_mode` 支持 general/gb18030/smart 三种模式
- [x] **R23** 候选窗样式 — 支持水平（单行）和垂直布局，主题系统
- [x] **R24** 编码显示位置 — `show_code_hint` 在候选窗内显示编码提示

### 部分实现 🔶

- [ ] **R16** 四码空码处理策略 — `ClearOnEmptyAt4` 支持清空，缺少"保留候选窗"和"转临时英文"策略
- [ ] **R19** 快捷键删词 — Shadow层支持 `ShadowActionDelete`，但未确认是否有快捷键绑定
- [ ] **R20** 快捷键调词序 — Shadow层支持 `ShadowActionTop/Reweight`，未确认快捷键绑定

### 未实现 ❌

- [ ] **R18** 快捷键加词 — 未发现快捷键加词功能
- [ ] **R22** 生僻字词自定义 — 依赖系统 `IsCommon` 标志，不支持用户自定义标记

---

## P2 可选增强（4项）

- [ ] **R25** 输入模式扩展 — 部分实现，有拼音引擎但扩展能力有限
- [ ] **R26** 词库维护平台化 — 词库分层但无统一维护/同步能力
- [ ] **R27** 生僻字词标记快捷键 — 未实现
- [ ] **R28** 候选数与快捷键深度自定义 — 部分实现，候选数/选择键组/翻页键可配，非完全自由自定义

---

## 默认关闭项（6项，均符合预期）

- [x] **N01** 编码逐渐提示 — `show_code_hint` 可配置关闭
- [x] **N02** 词组联想 — 未实现，符合"关闭"预期
- [x] **N03** 标点自动配对 — 未实现，符合"关闭"预期
- [x] **N04** 空码时检索生僻字 — 未实现，符合"关闭"预期
- [x] **N05** 临时英文快捷键 — Shift+字母触发，可关闭
- [x] **N06** 简入繁出 — 未实现，符合"关闭"预期

---

## 优先级建议

### 第一优先：P0 缺口

1. **R01 词库导入导出** — 核心数据可迁移性，影响用户留存
2. **R14 z键重复录入** — 五笔老用户基础功能
3. **R04 数字后句号行为** — 简单配置项，实现成本低
4. **R15 单候选时选重键行为** — 避免误触，体验细节
5. **R02 自定义标点** — 标点映射从硬编码改为可配置
6. **R12 补齐生僻字引导** — 临时拼音已有，补齐生僻字入口
7. **R13 回车键行为完善** — 确认并补齐配置项

### 第二优先：P1 缺口

1. **R18 快捷键加词** — 词库维护效率
2. **R19/R20 确认快捷键绑定** — Shadow层后端已支持，需确认前端入口
3. **R16 空码处理策略完善** — 补齐"保留候选窗"等策略
4. **R22 生僻字词自定义** — 用户个性化过滤

### 已有亮点

- 四码自动上屏、五码顶字、标点顶字等核心输入行为完善
- 候选窗支持水平/垂直布局、主题系统
- 中英切换、临时拼音等交互完整
- 词库四层架构设计合理（短语/shadow/用户/系统）
